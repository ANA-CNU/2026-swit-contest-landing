# SW-IT Contest 2026 — 홍보 정적 사이트

## 프로젝트 성격

충남대학교 컴퓨터인공지능학부 알고리즘 동아리 ANA가 주최하는 SW-IT Contest 2026의
홍보용 웹사이트다. 원페이지 스크롤 구성이며, 목적은 두 가지뿐이다.

1. 포스터에 실린 대회 정보를 전달한다.
2. 참가 신청 구글폼으로 보낸다.

DB, 인증, 폼 처리, 서버 로직은 없다. 외부 API 연동도 없다.
런타임에 불러오는 외부 자원은 Pretendard 웹폰트 CDN 하나뿐이다.
빌드 결과물은 순수 정적 파일이며 `2026-swit-contest.anacnu.kr`에 서빙된다.

## 스택

- **Astro** — 정적 출력. SSR 어댑터를 추가하지 않는다.
- **Tailwind CSS** — 아래 디자인 토큰을 `tailwind.config`에 등록해 사용한다.
- 상태 관리 라이브러리, UI 프레임워크, 애니메이션 라이브러리를 추가하지 않는다.
  애니메이션은 CSS와 최소한의 vanilla JS로 구현한다.
- 이미지 최적화는 Astro의 `<Image>` 또는 빌드 스크립트로 처리한다.

## 디자인 토큰

포스터 원본(3175×4490) 픽셀을 직접 샘플링한 값이다. 임의로 바꾸지 않는다.

```
orange       #FB8227   광선, 사람 실루엣, 주요 강조
orange-title #FD7E21   제목 CONTEST
orange-soft  #FDB35E   광선 밝은 톤, 보조
gold         #F7B03E   제목 SW-IT, 옥수수 알
paper        #FFFFFF   배경
ink          #000000   본문 텍스트
```

배경은 흰색이다. 다크 테마를 만들지 않는다.

### 서체

- 본문·UI: **Pretendard**. 포스터 본문과 동일한 서체다.
  jsDelivr CDN의 dynamic subset 버전을 `src/styles/global.css`에서 `@import`하고,
  `tailwind.config.mjs`의 `fontFamily.sans` 첫 항목으로 등록한다.
- 제목 `SW-IT CONTEST`: 포스터에 쓰인 서체는 **CDR제스트**이며,
  라이선스가 임베딩과 웹폰트 사용을 제외하고 있다. `@font-face`로 배포할 수 없다.
  따라서 제목은 **아웃라인 처리된 SVG 에셋**으로 쓴다. 원본은
  `asset/SOURCE_title/title_swit.svg`, `asset/SOURCE_title/title_contest.svg` 두 개이며
  전처리 단계에서 `public/assets/`로 복사한다.
  SVG 내부에 `<text>`나 폰트 참조가 남아서는 안 된다. 반드시 패스로 변환된 상태여야 한다.
  대체 웹폰트로 근사하려 시도하지 않는다.
- 숫자·날짜·시간에 별도 모노스페이스를 쓰지 않는다. 포스터에 그런 처리가 없다.

## 에셋

원본은 `asset/` 아래 `SOURCE_*` 폴더에 종류별로 있다. **읽기 전용이며 절대 수정하지 않는다.**

```
asset/SOURCE_corn/      corn_1..7.png        옥수수 알 7종
asset/SOURCE_keycab/    keycab_1..8.png      키캡 8종 (^ v [ ] i * ↗ ↙)
asset/SOURCE_pixel/     pixel_1..8.png       픽셀 디졸브 조각 8종
asset/SOURCE_popcorn/   popcorn_1..12.png    팝콘 12종
asset/SOURCE_sparkle/   sparkle_1..16.png    반짝임 16종 (1~8 단일, 9~16 클러스터)
asset/SOURCE_figure/    figure_1..4.png      사람 실루엣 4종
asset/SOURCE_title/     title_swit.svg       제목 로고 SW-IT
                        title_contest.svg    제목 로고 CONTEST
```

PNG 원본은 55개, 제목 SVG는 2개다.

### 전처리 규칙

원본 PNG는 캔버스에 여백이 있다. 예: `corn_1.png`는 720×994이나 실제 불투명
영역은 (98,189)~(633,838)이다. 그대로 쓰면 크기와 회전 중심이 어긋난다.

- 빌드 전 **알파 바운딩 박스로 자동 크롭**한 뒤 WebP로 변환해 `public/assets/`에 둔다.
- 변환은 `scripts/build-assets.py`가 담당한다. 결과는 커밋하지 않는다.
- 크롭 후 종횡비를 매니페스트에 기록한다. 레이아웃 계산에 필요하다.

### 사용 방침

에셋은 총 55개지만 전부 쓰지 않는다.

- 히어로에 배치하는 오브젝트는 **최대 30개**로 제한한다. 동시 애니메이션 부하와
  초기 로딩 용량 때문이다.
- `figure_1`(서 있는 실루엣)만 쓴다. `figure_2`는 뿜어지는 요소가 함께 그려져 있어
  개별 오브젝트와 중복된다.
- `pixel_*`은 **사용하지 않는다.** 광선 레이어에도 오브젝트 레이어에도 넣지 않는다.
  전처리 스크립트는 계속 WebP 를 생성하지만 페이지에서 참조하지 않는다.

## 히어로 애니메이션

포스터 상단 일러스트가 애니메이션의 **종료 상태**다. 페이지 로드 시 오브젝트가
사람 머리 지점에서 뿜어져 나와 최종 구도로 정착한다.

### 구도

포스터는 A3 세로 비율이고 웹 히어로는 가로로 넓다. 포스터 좌표를 그대로 옮기지
않는다. 대신 원점 기준 **극좌표 규칙**으로 배치한다.

- 각도 `-8°` ~ `62°` — 우상향 부채꼴. 등방성 분사가 아니다.
- 거리 — 스테이지 대각선의 `12%` ~ `78%`
- 크기 — 거리에 비례해 증가한다. 원점에 가까운 것이 작고 먼 것이 크다(원근).

각 오브젝트의 각도·거리·크기는 **상수 배열에 하드코딩**한다.
`Math.random()`을 쓰지 않는다. 새로고침마다 구도가 바뀌면 안 된다.
광선 각도와 길이도 동일하게 상수로 둔다.

### 구현 제약

- **`transform`과 `opacity`만 애니메이션한다.** `left`/`top`/`width`는 정적으로 배치하고
  건드리지 않는다. 오브젝트가 30개인 상황에서 리플로우가 발생하면 모바일에서 무너진다.
- 초기 상태는 "최종 위치에서 원점까지의 벡터만큼 밀어놓고 `scale`을 축소",
  최종 상태는 `transform: none`이다.
- 각 오브젝트를 두 겹으로 감싼다. 바깥은 이동, 안쪽은 회전만 담당하며 서로 다른
  easing을 준다. 직선 궤적으로 보이지 않게 하는 목적이다.
- 이동에 overshoot easing을 적용한다.
- 스태거는 원점에서 가까운 것부터다. 총 진행 시간 1.4초 이내.
- 광선은 `transform-origin`을 원점에 두고 `scaleX`로 뻗는다. 오브젝트보다 먼저 시작한다.
- `will-change`는 진행 중에만 걸고 `animationend`에서 제거한다.
- 크롭된 원본은 회전이 픽셀에 구워져 있다. 최종 회전값은 매니페스트에 두지 않고,
  회전은 비행 중 스핀에만 쓴다.
- 스테이지 전체에 `aria-hidden="true"`를 준다. 장식이므로 스크린리더에 노출하지 않는다.

### 필수 대응

- `prefers-reduced-motion: reduce` — 애니메이션 없이 최종 상태를 즉시 렌더한다.
- 뷰포트 640px 이하 — 애니메이션을 생략하고 최종 상태만 보여준다.
- 오브젝트 `img`에 `loading="eager"`, `decoding="async"`, `alt=""`를 설정한다.

## 대회 정보 (포스터 확정 내용)

페이지 콘텐츠는 아래를 기준으로 한다. 임의로 추가하거나 바꾸지 않는다.
비어 있는 항목은 플레이스홀더로 두고 `TODO` 주석을 남긴다.

- **대회명** SW-IT Contest 2026
- **주최** 충남대학교 컴퓨터인공지능학부 알고리즘 동아리 ANA
- **성격** 팀 단위 알고리즘 문제 해결 대회. **3인 1팀**
- **일자** 2026.10.05
- **장소** 충남대학교 제3학생회관 (N-7) 1층 영탑홀
- **접수 기간** 09.08 ~ 09.27
- **참가 자격** 충남대학교 및 대전·충청 지역 소재 대학 재학생,
  COSS 컨소시엄 소속 대학 재학생

### 진행 일정

```
12:00 ~ 13:00   특강
13:00 ~ 13:30   휴식 및 대회 OT
13:30 ~ 16:30   대회 진행
16:30 ~ 18:00   문제 해설 및 시상
```

### 시상

```
대상   600,000원   1팀
금상   400,000원   2팀
은상   200,000원   3팀
동상   100,000원   5팀
```

수상자에게는 상금과 함께 '충남대학교데이터보안활용 혁신융합대학사업단장상'이 수여된다.

### 대회 규칙

- 온라인 저지는 **AOJ (ANA 온라인 저지 사이트)** 다.
- 대회 전용 계정은 사전에 발부한다. 참가자는 그 계정으로 AOJ 에 접속한다.
- 본인이 지참한 노트북으로 문제를 푼다.
- 평가는 **ICPC 기준**이다. 더 많은 문제를 더 빨리 푼 팀이 우위를 가진다.
- 프로그래밍 언어 선택 자유, IDE 사용 가능, 사전 작성한 팀 노트 허용.
- ChatGPT, Gemini, Copilot, Claude 등 소스 코드를 자동 생성하는 서비스 사용 금지.

### 로고

푸터에 넷을 배치한다 — ANA, 충남대학교, 소프트웨어중심대학사업단, COSS.

## 배포

- `npm run build` 결과인 `dist/`를 정적 서빙한다.
- 대상은 ANA 동아리 서버(Ubuntu 24.04, nginx)의 `2026-swit-contest.anacnu.kr`이다.
- `astro.config`의 `site`에 위 주소를 지정한다. sitemap과 OG 절대 URL 생성에 필요하다.
- OG 이미지는 포스터를 1200×630으로 크롭해 `public/og-image.png`로 둔다.

## 금지 사항

- 대회 정보를 추측해 채우지 않는다. 확정되지 않은 값은 `TODO` 주석과 함께 비워둔다.
- `asset/`을 수정하지 않는다. 원본 에셋 디렉터리는 읽기 전용이다.
- 애니메이션에 난수를 쓰지 않는다.
- 다크 테마, 백엔드, 외부 API 연동을 추가하지 않는다.
- 애니메이션 라이브러리를 설치하지 않는다.
